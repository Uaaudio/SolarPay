<%- include('../partials/header') %>

<div class="container my-5">
  
  <!-- Header Section -->
  <div class="row mb-5 align-items-center">
    <div class="col-md-8">
      <h1 class="fw-bold display-6">Olá, <span><%= user.userName %></span>! 👋</h1>
      <p class="text-muted lead mb-0">Bem-vindo ao seu painel do morador.</p>
    </div>
    <div class="col-md-4 text-md-end mt-3 mt-md-0">
      <% if(user.admin){ %>
        <a href="/admin/seedashboard/<%= user.id %>" class="btn-secondary-theme">
          <i class="bi bi-speedometer2 me-2"></i>Painel Admin
        </a>
      <% } %>
    </div>
  </div>

  <!-- Action Cards -->
  <div class="row justify-content-center g-4">
    
    <div class="col-md-6 col-lg-5">
      <div class="card h-100">
        <div class="card-body text-center p-5">
          <div class="mb-4">
            <div class="icon-circle mx-auto d-flex align-items-center justify-content-center" style="width: 80px; height: 80px; background-color: var(--sand-beige); border-radius: 50%; border: 2px dashed var(--solar-orange);">
              <i class="bi bi-houses fs-1 icon-warning"></i>
            </div>
          </div>
          <h3 class="card-title fw-bold">Meus Imóveis</h3>
          <p class="card-text text-muted mb-4">Visualize seus apartamentos, consulte faturas pendentes e histórico de pagamentos.</p>
          <a href="/home/seehouses/<%= user.id%>" class="btn">
            Acessar Imóveis
          </a>
        </div>
      </div>
    </div>

  </div>
</div>

<%- include('../partials/footer') %>
