<%- include('../partials/header') %>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">

      <div class="card">
        <div class="card-body p-4 text-center">
          <h2 class="mb-4"><i class="bi bi-qr-code-scan me-2"></i>Pagamento via Pix</h2>
          
          <div class="mb-4">
            <p class="text-muted mb-1">Referente ao mês de</p>
            <h3 class="fw-bold text-dark"><%= feeInfo.month %>/<%= feeInfo.year %></h3>
            <span class="badge mt-2" style="background-color: var(--sand-beige); color: var(--text-dark); border: 1px solid var(--solar-orange);">Apartamento <%= feeInfo.House ? feeInfo.House.number : 'N/A' %></span>
          </div>

          <div class="p-3 rounded mb-4 d-inline-block" style="background-color: var(--card-bg); border: 2px dashed var(--solar-orange);">
            <img src="/img/myqrcode.jpeg" alt="QR Code Pix" class="img-fluid" style="max-width: 200px; height: auto;">
          </div>

          <div class="mb-4">
            <p class="text-muted mb-0">Valor a pagar</p>
            <h2 class="fw-bold" style="color: var(--tropical-green);">R$ 150,00</h2>
          </div>

          <div class="d-grid gap-2">
            <form method="post" action="/payment/confirm/<%= fee %>">
              <button type="submit" class="btn btn-lg w-100">
                <i class="bi bi-check-circle-fill me-2"></i>Confirmar Pagamento
              </button>
            </form>
            <a href="/home/housedetails/<%= UserId %>/<%= feeInfo.houseId %>" class="btn-secondary-theme text-center text-decoration-none">
              Cancelar
            </a>
          </div>

        </div>
        <div class="card-footer text-center text-muted small py-3">
          Após o pagamento, clique em confirmar para baixar a pendência.
        </div>
      </div>

    </div>
  </div>
</div>

<%- include('../partials/footer') %>
